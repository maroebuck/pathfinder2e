version: 2
jobs:
  build:
    working_directory: ~/my-project/Pathfinder/ClientApp
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run: npm install
      - run: npm run test -- --no-watch --no-progress --browsers=ChromeHeadlessCI